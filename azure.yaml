# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: nocturne
services:  
  app:
    language: dotnet
    project: ./src/Aspire/Nocturne.Aspire.Host/Nocturne.Aspire.Host.csproj
    host: containerapp

{
  "name": "transform-api-client",
  "version": "1.0.0",
  "type": "module",
  "description": "Post-processing script to transform NSwag generated TypeScript client to use object parameters",
  "main": "index.ts",
  "scripts": {
    "generate-then-transform": "npm run generate-api-client && npm run transform",
    "transform": "node --experimental-transform-types index.ts",
    "generate-api-client": "cd ../../../API/Nocturne.API && dotnet restore && dotnet build && nswag run nswag.json"
  },
  "packageManager": "pnpm@8.0.0",
  "devDependencies": {
    "@types/node": "^22.16.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=24"
  }
}
